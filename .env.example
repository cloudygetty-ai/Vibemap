# ============================================================
# VibeMap Pro — Environment Variables
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.
# ============================================================

# --- Server ---

# PostgreSQL connection string (with PostGIS enabled)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql://vibemap:changeme@localhost:5432/vibemap

# Redis connection string
# Format: redis://[:PASSWORD@]HOST:PORT
REDIS_URL=redis://localhost:6379

# Port the Express server listens on (default: 4000)
PORT=4000

# Allowed CORS origin for Socket.io (comma-separated, or * for all)
# Example: https://app.vibemap.io,https://www.vibemap.io
CORS_ORIGIN=http://localhost:3000

# --- Client (Next.js public vars — exposed to the browser) ---

# Mapbox GL JS public token (get one at https://account.mapbox.com)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_token_here

# Full URL of the running backend server
NEXT_PUBLIC_SERVER_URL=http://localhost:4000

# Google Maps / Street View API key (https://console.cloud.google.com)
NEXT_PUBLIC_GOOGLE_KEY=your_google_maps_api_key_here

{
  "name": "vibemap-server",
  "version": "1.0.0",
  "description": "VibeMap Pro - Backend API and Socket.io server",
  "main": "index.js",
  "scripts": {
    "start": "node ./db/migrate.js && node index.js",
    "dev": "node ./db/migrate.js && nodemon index.js",
    "test": "node --test test/validation.test.js",
    "migrate": "node ./db/migrate.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "redis": "^4.6.10",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "pino": "^8.17.0",
    "pino-http": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
